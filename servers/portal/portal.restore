#!/bin/bash

sudo apt update

BASE_DIR=/home/portal
sudo mkdir -p $BASE_DIR
sudo chown -R $(id -u):$(id -g) $BASE_DIR
kirios restore folder "$BASE_DIR/vikunja portal vikunja 20240530"
kirios restore folder "$BASE_DIR/healthchecks portal healthchecks 20240524"